import React from 'react';

const MatchPredictor = () => {
    return(
        <div className="Match-Predictor">
            <h1>Match Predictor</h1>
            <p><i>Match prediction will starts once we have collected enough data from the exhibitiion matches proir to the beginning of the <b>KCAC Conference</b> starts in september of 2022.</i></p>
        </div>
    )
};

export default MatchPredictor;